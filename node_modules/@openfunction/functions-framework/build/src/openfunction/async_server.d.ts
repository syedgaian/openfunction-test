import { DaprServer } from '@dapr/dapr';
import { OpenFunction } from '../functions';
import { OpenFunctionContext } from './context';
export declare type AsyncFunctionServer = DaprServer;
/**
 * Creates and configures an Dapr server and returns an HTTP server
 * which will run it.
 * @param userFunction User's function.
 * @param context Context of user's function.
 * @return HTTP server.
 */
export default function (userFunction: OpenFunction, context: OpenFunctionContext): AsyncFunctionServer;
//# sourceMappingURL=async_server.d.ts.map